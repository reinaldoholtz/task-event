DROP TABLE Event IF exists;

CREATE TABLE Event (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY (START WITH 1, INCREMENT BY 1) NOT NULL,
  idevent VARCHAR(20) NOT NULL,
  duration BIGINT NOT NULL,
  eventtype VARCHAR(30),
  host VARCHAR(10),
  alert BOOLEAN,
  PRIMARY KEY (id)
);